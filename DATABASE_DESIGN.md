# تصميم بيانات نواصرة التشغيلي

## مصدر المخطط

المصدر الوحيد للمخطط هو migrations في `supabase/migrations` من `001` حتى `094`. لا تطبق SQL يدويًا من وثيقة قديمة، ولا تستخدم `supabase db reset` على قاعدة الإنتاج. قبل أي تغيير:

```powershell
npx supabase migration list
```

يجب أن تتطابق الهجرات المحلية والبعيدة، ثم تضاف migration جديدة متسلسلة فقط.

## الإضافات المعتمدة حتى 094

| migrations | الإضافة |
|---|---|
| `063` | صور أغلفة الأقسام التي يديرها صاحب العمل وتظهر في المتجر. |
| `064`–`065` | إدارة حسابات الموظفين بأمان ودعم أكثر من مالك نظام مع منع فقدان آخر مالك. |
| `066`–`069` | مساعد الإدارة الآمن، ثم قراءات المخزون والتقارير التشغيلية وأسعار البيع من دوال مقيدة الصلاحية. |
| `070`–`072` | عائلات النكهات بسعر تجاري مشترك ومخزون مستقل لكل نكهة، مع الإنشاء والإدارة والترتيب الذري. |
| `073` | فهارس أداء للصفحة الرئيسية وتجميع المخزون وسجل حالات الطلبات. |
| `074` | RPCs المستودع الحساسة والملف الشخصي الذاتي مع صلاحيات مقيدة. |
| `075`–`077` | نماذج قراءة خادمية مقسطة للطلبات التشغيلية والمنتجات والمخزون وCRM بدل تحميل السجل كاملًا للواجهة. |
| `078` | سلامة محاسبية وتشغيلية: ورديات وإلغاءات وتسويات مدققة. |
| `079` | إزالة RPCs مستودع متقاعدة بعد التحقق من عدم وجود مراجع تشغيلية لها. |
| `080` | حماية idempotency لدفعات الموردين ومنع تكرار التسجيل عند إعادة الإرسال. |
| `081` | pagination والبحث والفلترة الخادمية الكاملة للكتالوج العام. |
| `082` | نماذج قراءة إدارة خادمية مقسطة لأوامر الشراء وسندات الاستلام ودفعات الموردين وذمم العملاء، مع فهارس وصلاحيات تنفيذ مقيدة. |
| `083`–`084` | primitives مدققة لعكس بيع POS ودفعة المورد، ثم preview وorchestrator ذري لعكس الوردية للمالك مع AAL2 ومنع الحالات غير الآمنة. |
| `085` | إدخال بيع POS الآجل في نموذج الذمم، وتفاصيل عميل مقسطة، وبحث POS خادمي كامل للعملاء. |
| `086`–`087` | بوابة طلب الضيف المحمية بحدود إساءة الاستخدام، ثم توحيد حد طلب الجملة عند 50 بندًا في الواجهة والعقد الخاص. |
| `088`–`090` | استجابة merchandising محدودة واحدة، وإرجاع رمز التتبع مع الطلب بأمان، وجعل تفاصيل التوصيل الحرة اختيارية مع بقاء العنوان المنظم مطلوبًا. |
| `091` | انتهاء حجز طلب `website/new` بعد خمس ساعات عبر عملية ذرية خاصة وCron كل خمس دقائق، من دون backfill للطلبات التاريخية. |
| `092` | حذف محدود لسجلات rate-limit الأقدم من 48 ساعة عبر Cron، مع إبقاء أطول نافذة حماية ومدة idempotency. |
| `093` | أرشيف ورديات خادمي مقسّط ومحمي مع فلاتر التاريخ والفرع والكاشير والحالة والرقم. |
| `094` | لقطة JSON immutable لتقرير إغلاق الورديات الجديدة داخل معاملة الإغلاق؛ الورديات المغلقة القديمة تستمر بالحساب legacy دون backfill. |

المساعد لا يقرأ الجداول مباشرة من المتصفح، ولا يملك `service_role`. سياق العمل يأتي من RPCs محمية وتبقى إجابات الذكاء الاصطناعي للقراءة والمساعدة، وليست مصدرًا محاسبيًا أو أمرًا يغير البيانات.

## مجالات البيانات

| المجال | أمثلة على مصدر الحقيقة | القاعدة التشغيلية |
|---|---|---|
| المرجعيات | الفروع، المستودعات، الأقسام، الماركات، الوحدات والمنتجات | تُدار من الإدارة وبصلاحية محمية. |
| المخزون | `inventory_balances` و`inventory_movements` | الرصيد لا يعدل من المتصفح؛ كل تغيير حركة مدققة. |
| التوريد | الموردون، سندات الاستلام، دفعات المورد | استلام الطرد يحول محتواه إلى وحدات أساس ويحدث الذمة ذريًا. |
| الطلبات | الطلبات، البنود، الحجوزات وحالات التنفيذ | طلب العميل العام يمر عبر RPC محمية بمفتاح منع التكرار. |
| العملاء والذمم | العميل، العناوين، تحصيلات الطلبات وسندات القبض | رقم الهاتف للربط التجاري؛ UUID هو الهوية الداخلية. |
| البيع المباشر | إيصالات POS، التحصيل، المرتجعات | لا يتم البيع إلا من وردية مفتوحة وبكمية طرود صحيحة. |
| التشغيل المالي | المصروفات، الورديات، أرشيف الورديات وتقارير الإغلاق والتقارير الدورية | الكاش وCliQ منفصلان، والفروقات تحتاج سببًا محفوظًا، ولقطة الإغلاق الجديدة immutable. |
| التنبيهات | Outbox أحداث الأتمتة وسجل التسليم | التسجيل داخل PostgreSQL؛ القنوات الخارجية لا تعدل ERP. |

## المخزون والطرود

- وحدة المخزون داخليًا هي **الوحدة الأساسية** فقط.
- المنتج يعرّف طرد شراء وطرد بيع ومحتوى كل منهما.
- إدخال 3 كراتين، وكل كرتونة فيها 12 حبة، يسجل 36 وحدة أساس مع حركة استلام واحدة قابلة للتدقيق.
- البيع للعميل يكون بطرود البيع الكاملة فقط؛ الواجهة لا تقدم بيع تجزئة بالحبة.
- الجرد يقارن العدد الفعلي بالرصيد المتوقع وينشئ تسوية مدققة، ولا يمسح سجل الحركات.

## المال والدقة الحسابية

- المبالغ تخزن وتُحسب بوحدات صحيحة من ألف الدينار الأردني، ثم تُعرض بدقة ثلاثة منازل (`د.أ`). لا يعتمد النظام على جمع JavaScript العائم للحقائق المحاسبية.
- تكلفة طرد البيع مشتقة من تكلفة الوحدة الأساسية ومحتوى طرد البيع.
- رسوم التوصيل والخصومات وطريقة الدفع والتحصيل الجزئي تُثبت في قاعدة البيانات نفسها؛ لا يرسل المتصفح رقم خصم أو رسمًا يفرضه بنفسه.
- الذمم لا تصبح سالبة، وسندات التحصيل/الدفع مرتبطة بالوردية المناسبة حيث يلزم.

## دورة الطلب

```text
كتالوج عام آمن
  -> RPC طلب ضيف مع idempotency
  -> عميل مرتبط بالهاتف + طلب + حجز مخزون
  -> انتهاء ذري بعد 5 ساعات إذا بقي طلب الموقع new، أو تأكيد يحفظ الحجز
  -> تأكيد وتجهيز
  -> بدء توصيل (وقت متوقع + رقم سائق)
  -> تحصيل/تسوية أو ذمة
  -> تسليم أو إلغاء/مرتجع مدقق
```

رابط التتبع والإيصال العامان يستخدمان رموزًا عشوائية لكل طلب ولا يعرضان عنوان العميل أو هاتفه أو التكلفة أو الربح.

## الصلاحيات والتدقيق

- RLS تحمي الجداول؛ القراءة العامة محدودة بالكتالوج والعروض وتتبع الطلب/الإيصال المصرح بهما فقط.
- دوال ERP التي تغير المخزون أو المال تتحقق من الهوية والدور، وتطبق MFA عند الحاجة.
- كل عكس أو إلغاء يبقي السجل واضحًا: لا حذف صامت لحركة مالية أو مخزنية مؤثرة.
- `service_role` محصور داخل Edge Functions الموثقة، ولا يرسل إلى React أو n8n.

## استمرارية البيانات

- النسخ الاحتياطي يتضمن بنية PostgreSQL، البيانات، الأدوار وملفات Storage داخل أرشيف مشفر.
- مهمة Windows الليلية تعمل كـ`SYSTEM` بأدوات PostgreSQL الأصلية، وتنشئ الأرشيف دون جلسة مستخدم أو Docker؛ فحص السلامة يتحقق من التشفير والبصمات.
- استعادة الاختبار تُشغل في Docker معزول ولا تلمس Supabase الحية.
